<%= form_for @neglected_term, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="control-group">
    <%= f.label :exclude, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :exclude, :class => 'text_field' %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                neglected_terms_path, :class => 'btn' %>
  </div>
<% end %>
