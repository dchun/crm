<%= form_for @acceptable_contact_position, :html => { :class => 'form-horizontal' } do |f| %>

  <% if @acceptable_contact_position.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@acceptable_contact_position.errors.count, "error") %> prohibited this acceptable_contact_position from being saved:</h2>

      <ul>
      <% @acceptable_contact_position.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <div class="control-group">
    <%= f.label :position, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :position, :class => 'text_field' %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                acceptable_contact_positions_path, :class => 'btn' %>
  </div>
<% end %>
