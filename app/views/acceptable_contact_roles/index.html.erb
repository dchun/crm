<%- model_class = AcceptableContactRole -%>
<div class="page-header">
  <h1><%=t '.title', :default => model_class.model_name.human.pluralize.titleize %></h1>
</div>
<table class="table table-striped">
  <thead>
    <tr>
      <th><%= model_class.human_attribute_name(:id) %></th>
      <th><%= model_class.human_attribute_name(:role) %></th>
      <th><%= model_class.human_attribute_name(:created_at) %></th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @acceptable_contact_roles.each do |acceptable_contact_role| %>
      <tr>
        <td><%= link_to acceptable_contact_role.id, acceptable_contact_role_path(acceptable_contact_role) %></td>
        <td><%= acceptable_contact_role.role %></td>
        <td><%=l acceptable_contact_role.created_at %></td>
        <td>
          <%= link_to t('.edit', :default => t("helpers.links.edit")),
                      edit_acceptable_contact_role_path(acceptable_contact_role), :class => 'btn btn-mini' %>
          <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
                      acceptable_contact_role_path(acceptable_contact_role),
                      :method => :delete,
                      :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                      :class => 'btn btn-mini btn-danger' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to t('.new', :default => t("helpers.links.new")),
            new_acceptable_contact_role_path,
            :class => 'btn btn-primary' %>
